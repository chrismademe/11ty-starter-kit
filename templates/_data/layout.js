/**
 * Set the default layout so you don't need to
 * set it on every template.
 *
 * If you don't want to wrap your content in a layout,
 * set layout: false in the template's frontmatter
 */
module.exports = 'default';